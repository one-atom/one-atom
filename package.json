{
  "private": true,
  "license": "MIT",
  "scripts": {
    "boot": "lerna bootstrap",
    "dev": "node run.dev",
    "build": "tsc --build",
    "pretest": "yarn build",
    "test": "jest",
    "clean": "rimraf modules/*/{tsconfig.tsbuildinfo,lib,dist}"
  },
  "devDependencies": {
    "@types/jest": "^25.2.2",
    "@types/json5": "^0.0.30",
    "@types/mock-fs": "^4.10.0",
    "@types/node": "^14.0.26",
    "inquirer": "^7.3.3",
    "jest": ">=26.0.1",
    "mock-fs": "^4.12.0",
    "rimraf": "^3.0.2",
    "ts-jest": "^25.5.1",
    "typescript": "^3.9.7"
  }
}
